import 'package:flutter/material.dart';

class ConfNav extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Column(
        children: [Text("Nav3333")],
      ),

    );
  }
}
