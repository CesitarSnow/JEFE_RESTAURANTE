import 'package:flutter/material.dart';

class CarritoNav extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Column(
        children: [Text("Nav44444")],
      ),

    );
  }
}
