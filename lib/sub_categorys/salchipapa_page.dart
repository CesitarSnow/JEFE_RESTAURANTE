import 'package:flutter/material.dart';

class SalchipapasPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
            children: [
              Text("Estas en la categoria de Salchipapa")
            ],
        ),
      ),
      
    );
  }
}
